import anthropic

client = anthropic.Anthropic()

class DataGenerator:

    # We want a class to generate data obvi

    # We proably want something to get the data from
    # metadata, then go over the categories of the diff prompts